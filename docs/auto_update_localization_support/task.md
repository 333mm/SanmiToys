# タスク管理: 多言語辞書内の不要な英語括弧表記（(Hold to Enable)等）の全数クリーンアップ

## タスク一覧
- [x] 1. `LocalizationService.cs` の各言語辞書（ja, en, zh-Hans, zh-Hant, ko 等）から不要な英語補足括弧表記を全数削除 <!-- id: 0 -->
    - [x] `FluidDrag_ModifierKey`: `(Hold to Enable)` 削除
    - [x] `FluidDrag_ResizeKey`: `(Hold to Disable)` 削除
    - [x] `FocusDimmer_DimDesktopOnly`: `(Dim Desktop Only)` 削除
    - [x] `FocusDimmer_ExcludeTopmost`: `(Topmost)` 削除
    - [x] `FocusDimmer_Inspector_Title`: `(Inspector)` 削除
    - [x] `FocusDimmer_AlwaysBright_Title`: `(Always Bright)` 削除
    - [x] `FocusDimmer_AlwaysDark_Title`: `(Always Dark)` 削除
    - [x] `SnapTrans_AutoSpeak`: `(TTS)` 削除
    - [x] `SnapTrans_TargetLang`: `(Target Language)` 削除
    - [x] `General_Support_Global_Title`: `(Buy Me a Coffee)` 削除
- [x] 2. ビルド検証（エラー・警告ゼロ確認） <!-- id: 1 -->
- [x] 3. ドキュメント（walkthrough.md）の更新 <!-- id: 2 -->
